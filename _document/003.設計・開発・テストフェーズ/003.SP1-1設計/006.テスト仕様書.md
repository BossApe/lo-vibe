# SP1-1 テスト仕様書（FR-001）

[← 005.DB設計書](005.DB設計書.md) | [一覧](../README.md) | [→ 007.エラーハンドリング設計書](007.エラーハンドリング設計書.md)

> **対象**: TK1-1-1 で実装した FR-001「システム概要入力・保存」の単体・結合テスト仕様

<details>
<summary>目次（クリックで展開）</summary>

- [1. テスト方針](#1-テスト方針)
- [2. テスト環境](#2-テスト環境)
- [3. 単体テスト](#3-単体テスト)
  - [3.1 Service 層 — system_overview_test.go](#31-service-層--system_overview_testgo)
  - [3.2 Handler 層 — system_overview_test.go](#32-handler-層--system_overview_testgo)
- [4. 結合テスト](#4-結合テスト)
  - [4.1 結合テスト — integration/system_overview_test.go](#41-結合テスト--integrationsystem_overview_testgo)
- [5. 受け入れ条件（AC）との対応](#5-受け入れ条件acとの対応)
- [6. テスト実行方法](#6-テスト実行方法)

</details>

---

## 1. テスト方針

| 方針 | 内容 |
| --- | --- |
| 単体テスト | Service 層・Handler 層を対象にモックを使用してテストする |
| 結合テスト | `httptest.Server` を使いルーティング〜ミドルウェア〜Handler の連結をテストする |
| DB 実機テスト | SP1-1 スコープでは実施しない（Repository 層はモック代替） |
| CI | `go test ./...` で全テストを実行し、全 PASS を確認する |
| カバレッジ目標 | Service 層・Handler 層 80% 以上 |

---

## 2. テスト環境

| 項目 | 内容 |
| --- | --- |
| 言語・バージョン | Go 1.22 |
| テストフレームワーク | 標準 `testing` + `testify/assert` + `testify/mock` |
| HTTP テスト | `net/http/httptest` |
| テストファイル配置 | 各パッケージ内（`_test.go`）/ `src/test/integration/` |

---

## 3. 単体テスト

### 3.1 Service 層 — system_overview_test.go

**ファイル**: `services/musuhi-api/src/internal/service/system_overview_test.go`

| # | テストケース名 | 入力 | 期待結果 | ステータス |
| --- | --- | --- | --- | --- |
| UT-SVC-001 | `Create_OK` | 通常のテキスト（`"テスト用システム概要テキスト"`） | `SystemOverview` が返る・エラーなし | ✅ PASS |
| UT-SVC-002 | `Create_EmptyContent` | 空文字 `""` | `ErrValidation` が返る・repo.Create 未呼び出し | ✅ PASS |
| UT-SVC-003 | `Create_TooLongContent` | 4097 文字（ひらがな） | `ErrValidation` が返る・repo.Create 未呼び出し | ✅ PASS |
| UT-SVC-004 | `GetByID_OK` | 有効な UUID 文字列 | `SystemOverview` が返る・エラーなし | ✅ PASS |
| UT-SVC-005 | `GetByID_InvalidUUID` | `"not-a-uuid"` | `ErrValidation` が返る・repo.FindByID 未呼び出し | ✅ PASS |

**未テスト（今後追加推奨）:**

| # | テストケース名 | 入力 | 期待結果 |
| --- | --- | --- | --- |
| UT-SVC-006 | `Create_DBError` | DB エラーをモックで注入 | エラーが伝播する |
| UT-SVC-007 | `GetByID_NotFound` | 存在しない UUID | `ErrNotFound` が返る |
| UT-SVC-008 | `Create_ExactMaxLength` | ちょうど 4096 文字 | 保存成功 |
| UT-SVC-009 | `Create_WhitespaceOnly` | `"   "` (空白のみ) | `ErrValidation`（現仕様では通過する点に注意） |

---

### 3.2 Handler 層 — system_overview_test.go

**ファイル**: `services/musuhi-api/src/internal/handler/system_overview_test.go`

| # | テストケース名 | リクエスト | 期待レスポンス | ステータス |
| --- | --- | --- | --- | --- |
| UT-HDL-001 | `Create_OK` | `POST` / body: `{"content":"テスト概要テキスト"}` | HTTP 201 / `data.id` が UUID | ✅ PASS |
| UT-HDL-002 | `Create_ValidationError` | `POST` / body: `{"content":""}` | HTTP 422 | ✅ PASS |
| UT-HDL-003 | `GetByID_OK` | `GET /{id}` / PathValue: 有効 UUID | HTTP 200 / `data.id` が一致 | ✅ PASS |
| UT-HDL-004 | `GetByID_NotFound` | `GET /{id}` / PathValue: 存在しない UUID | HTTP 404 | ✅ PASS |

**未テスト（今後追加推奨）:**

| # | テストケース名 | リクエスト | 期待レスポンス |
| --- | --- | --- | --- |
| UT-HDL-005 | `Create_BadJSON` | `POST` / body: `{invalid}` | HTTP 400 / `BAD_REQUEST` |
| UT-HDL-006 | `Create_InternalError` | Service がサーバーエラーを返す | HTTP 500 / `INTERNAL_ERROR` |
| UT-HDL-007 | `GetByID_InvalidUUID` | `GET /not-a-uuid` | HTTP 400 / `BAD_REQUEST` |
| UT-HDL-008 | `GetByID_EmptyID` | `GET /` / PathValue: `""` | HTTP 400 / `BAD_REQUEST` |

---

## 4. 結合テスト

### 4.1 結合テスト — integration/system_overview_test.go

**ファイル**: `services/musuhi-api/src/test/integration/system_overview_test.go`

構成: `httptest.NewServer` にルーティング・CORS・Logger ミドルウェアを組み込んで検証

| # | テストケース名 | リクエスト | 期待レスポンス | ステータス |
| --- | --- | --- | --- | --- |
| IT-001 | `HealthCheck` | `GET /health` | HTTP 200 | ✅ PASS |
| IT-002 | `CreateSystemOverview_OK` | `POST /api/v1/system-overviews` / 通常テキスト | HTTP 201 / `data.id`・`data.content` が正しい | ✅ PASS |
| IT-003 | `CreateSystemOverview_ValidationError` | `POST /api/v1/system-overviews` / `content: ""` | HTTP 422 / `error.code == "VALIDATION_ERROR"` | ✅ PASS |
| IT-004 | `GetByID_NotFound` | `GET /api/v1/system-overviews/{id}` / 存在しない UUID | HTTP 404 / `error.code == "NOT_FOUND"` | ✅ PASS |
| IT-005 | `CORS_Headers` | `OPTIONS /api/v1/system-overviews` | `Access-Control-Allow-Origin` ヘッダーが存在する | ✅ PASS |

**未テスト（今後追加推奨）:**

| # | テストケース名 | リクエスト | 期待レスポンス |
| --- | --- | --- | --- |
| IT-006 | `GetByID_OK` | `GET /api/v1/system-overviews/{id}` / 有効 UUID | HTTP 200 / `data.content` が一致 |
| IT-007 | `Logger_OutputsRequest` | 任意のリクエスト | stderr にリクエストログが出力される |
| IT-008 | `Content-Type_JSON` | `POST /api/v1/system-overviews` | レスポンスヘッダーに `Content-Type: application/json` |

---

## 5. 受け入れ条件（AC）との対応

Issue #25（TK1-1-1）の受け入れ条件との対応を示す。

| 受け入れ条件 | 対応テスト | 確認方法 |
| --- | --- | --- |
| AC-001: システム概要が DB に保存される | UT-SVC-001 / IT-002 | `data.id` が返ること・Service が `repo.Create` を呼ぶこと |
| 単体テストが pass する | UT-SVC-001〜005 / UT-HDL-001〜004 | `go test ./internal/...` |
| 結合テストが pass する | IT-001〜005 | `go test ./test/...` |

---

## 6. テスト実行方法

```bash
# すべてのテストを実行
cd services/musuhi-api/src
go test ./...

# 単体テストのみ
go test ./internal/...

# 結合テストのみ
go test ./test/...

# カバレッジ付き
go test ./internal/... -coverprofile=cover.out
go tool cover -html=cover.out
```
