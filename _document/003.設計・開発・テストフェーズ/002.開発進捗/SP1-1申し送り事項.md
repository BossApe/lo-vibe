# SP1-1 申し送り事項・改善要望

## 対象 Sprint / Ticket

| 項目 | 値 |
| --- | --- |
| Sprint | SP1-1 |
| 対象 Ticket | TK1-1-1（FR-001）、TK1-1-2（FR-002） |
| 記録日 | 2026-05-08 |

---

## 申し送り事項

### [申し送り-001] 外部結合テストの実施が未完了

| 項目 | 内容 |
| --- | --- |
| 分類 | テスト |
| 優先度 | 中 |
| 対象 Ticket | TK1-1-1（FR-001）、TK1-1-2（FR-002） |
| 内容 | 外部結合テスト（`test/external/`）は PostgreSQL DB 接続が必要なため、Docker Compose 起動環境での実施が必要。SP1-1 期間中は DB 未起動により未実施。次スプリントまたは Docker 環境整備後に実施すること。 |
| 実施コマンド | `go test -tags external ./test/external/... -v` |
| 関連ファイル | `004.テスト結果/003.外部結合テスト/FR-001外部結合テスト結果.txt`、`FR-002外部結合テスト結果.txt` |

---

## 改善要望

### [改善-001] GetByID レスポンスへの updatedAt フィールド追加検討

| 項目 | 内容 |
| --- | --- |
| 分類 | 機能改善 |
| 優先度 | 低 |
| 対象 Ticket | TK1-1-1（FR-001） |
| 内容 | 現在の `system_overviews` テーブルには `updated_at` カラムが存在しない。PUT 更新後に更新日時を返せると UI 側での表示に利用できる。次フェーズで DB スキーマに `updated_at timestamptz DEFAULT now()` を追加し、レスポンスに含めることを検討する。 |
| バックログ登録先 | GitHub Issues（次スプリント計画時） |

### [改善-002] 内部結合テストでのキャッシュ使用（(cached) 表示）の対応

| 項目 | 内容 |
| --- | --- |
| 分類 | テスト品質 |
| 優先度 | 低 |
| 対象 Ticket | TK1-1-1（FR-001）、TK1-1-2（FR-002） |
| 内容 | 内部結合テストの実行結果に `(cached)` と表示される場合がある。結果ファイルにキャッシュ実行が混在するため、Makefile の `internal-result` ターゲットに `-count=1` フラグを追加してキャッシュを無効化する対応を検討する。 |
| バックログ登録先 | GitHub Issues（次スプリント計画時） |

### [改善-003] SuggestName の名前候補ロジックのテスト補強

| 項目 | 内容 |
| --- | --- |
| 分類 | テスト品質 |
| 優先度 | 低 |
| 対象 Ticket | TK1-1-2（FR-002） |
| 内容 | `SuggestName` の単体テストは候補が返ってくることのみ検証しており、候補の内容（文字列の形式・意味的妥当性）は検証していない。ロジックが固定的なルールベースである間は許容範囲だが、将来的に AI 連携する場合はプロパティベーステストの導入を検討する。 |
| バックログ登録先 | GitHub Issues（AI 連携実装時） |
